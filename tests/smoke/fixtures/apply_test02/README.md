# Sanitized UOF Fixture

This fixture preserves the HTML structure and WebForms payload shape needed by the
characterization test. Hostnames, virtual paths, identities, UUIDs, ViewState,
EventValidation, and encrypted handler parameters are synthetic.

Do not commit an unmodified response captured from a real UOF deployment.
